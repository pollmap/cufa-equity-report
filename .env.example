# CUFA MCP 서버 API 토큰 (Bearer 토큰, "Bearer " 접두어 제외한 값만)
# 실제 .env 파일은 commit 금지 (.gitignore 처리됨)
CUFA_MCP_TOKEN=your_nexus_mcp_token_here

# Nexus MCP 서버 URL (예: http://your-mcp-host/mcp)
NEXUS_MCP_URL=
